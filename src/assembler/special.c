#include "assemble.h"

int lsl(char* arg1, char* arg2, char* arg3,
		char* arg4, char* arg5, char* arg6) {
	//THIS FUNCTION IS REDIRECTED TO MOV
	//CHECK SPEC PAGE 16
	return 0;
}

int andeq(char* arg1, char* arg2, char* arg3,
		  char* arg4, char* arg5, char* arg6) {
	//Return all 0s, halt command for emulator
	return 0;
}
